@if (Info is null)
{
    <div class="department-details department-details-empty h-100 d-flex flex-column justify-content-center align-items-center">
        <p>
            <h4 class="text-secondary">Department Details</h4>
            <i class="bi bi-body-text department-details-icon icon"></i>
            <span class="text-dark">Select department from the list to see more details.</span>
        </p>
    </div>
}
else
{
    @if (Loading)
    {
        <div class="department-details department-details-info h-100 d-flex flex-column justify-content-center align-items-center">
            <div class="spinner-border text-info" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <div>Loading...</div>
        </div>
    }
    else
    {
        <div class="department-details department-details-info">
            <fieldset class="px-2 pb-5">
                <legend><i class="bi bi-info-square"></i>More info</legend>
                <div class="row">
                    <div class="col-sm-12">
                        <label class="form-label fw-bolder"
                               for="Name">
                            Name
                        </label>
                        <input type="text"
                               class="form-control-plaintext hca-form-control-plaintext"
                               id="Name"
                               value="@Info.Name"
                               readonly />
                    </div>
                </div>
            </fieldset>
        </div>
    }
}
