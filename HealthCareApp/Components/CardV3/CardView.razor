@if (Link != string.Empty)
{
    <a href="@Link" class="card border-light text-muted">
        @ChildContent
    </a>
}
else
{
    <div class="card border-light text-muted">
        @ChildContent
    </div>
}
